.DS_Store
.idea/
.vscode/

# Node
node_modules/
coverage/
*.tsbuildinfo

# Build output is generated by `npm run build` (see .witboost/.gitignore for
# the toolkit's own bundle output). This repo intentionally KEEPS
# .github/agents/ and .github/skills/ tracked in git so the self-hosted
# Copilot agent keeps working without a build step.

# Scratch clones of real tech adapter repos, used as research material for
# deriving naming conventions and other reference docs. Not part of this repo.
research/tech-adapters/
