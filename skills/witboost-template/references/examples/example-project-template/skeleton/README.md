# ${{ values.name | dump }}

This repository contains the metadata and configurations of the ${{ values.name | dump }} Data Product.

The documentation can be found under the `docs` folder, it will also be accessible from the Witboost Builder and it contains the documentation related to the Data Product architecture that are of interest for the Data Product team.
